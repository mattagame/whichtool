whichtool inspect
  target   snapshot  header-params.json
  surface  0588f240a28c  3 tools  ~407 tokens (estimate, heuristic-bpe-v1)
           2 tools excluded by conforming clients: export_report, stream_events

Worst first
  1. error  x-mcp-header/invalid  export_report
            Tool `export_report` has an invalid `x-mcp-header` annotation, so a
            conforming Streamable HTTP client must exclude it from tools/list entirely:
            the model never sees this tool. `/properties/tenant`: `Tenant Id` is not a
            valid HTTP field-name token (RFC 9110 tchar); `/properties/tenant`: `number`
            parameters may not be mirrored into a header.
  2. error  x-mcp-header/invalid  stream_events
            Tool `stream_events` has an invalid `x-mcp-header` annotation, so a
            conforming Streamable HTTP client must exclude it from tools/list entirely:
            the model never sees this tool. `/properties/filters/items`: the property is
            not statically reachable, so the path has to be a chain of `properties` keys
            and must not pass through `items`, `$ref`, or a composition or conditional
            keyword.
  3. warn   annotations/read-only-with-mutating-name  execute_query
            Tool `execute_query` declares `readOnlyHint: true` but its name leads with
            `execute`, which reads as a write. Either the hint or the name is misleading
            the model.

Context cost  ~407 tokens total, mcp-neutral-compact
  tool           tokens  share  desc  schema
  stream_events    ~138    34%   ~21     ~97
  export_report    ~135    33%   ~20     ~95
  execute_query    ~134    33%   ~22     ~92
  Structural estimate, not a real vocabulary, and not provider-specific. Measured
  against the same four-tool surface it read 2.4x what OpenAI counted and 0.5x what
  Anthropic counted: the providers disagree with each other by more than this estimate
  disagrees with either. Use it to compare tools against each other and to spot bloat;
  for a bill, read the number your provider reports.

Annotations  3/3 tools annotated
  readOnlyHint 3/3   destructiveHint 3/3   idempotentHint 3/3   openWorldHint 3/3
  Annotations are hints, not guarantees: the MCP spec is explicit that a client must not
  rely on them as a security boundary. whichtool checks their internal consistency, not
  their truthfulness; only running the tool could establish that, and whichtool never
  does.

Lexical overlap  3 pairs scored
  No pair scores at or above 0.40. Highest: 0.23 (execute_query <-> export_report).

Deprecated features
  Checked against MCP 2026-07-28 (registry read 2026-08-17): none of the 6 deprecated
  features is visible in a tools/list response or a tool definition. They are all
  client-, authorization- or transport-level, so there is nothing on a surface to flag.
  The deprecated HTTP+SSE transport is refused by `--transport legacy-sse` instead.

Findings  2 errors, 1 warning, 1 info
  error  x-mcp-header/invalid  export_report
         Tool `export_report` has an invalid `x-mcp-header` annotation, so a conforming
         Streamable HTTP client must exclude it from tools/list entirely: the model
         never sees this tool. `/properties/tenant`: `Tenant Id` is not a valid HTTP
         field-name token (RFC 9110 tchar); `/properties/tenant`: `number` parameters
         may not be mirrored into a header.
  error  x-mcp-header/invalid  stream_events
         Tool `stream_events` has an invalid `x-mcp-header` annotation, so a conforming
         Streamable HTTP client must exclude it from tools/list entirely: the model
         never sees this tool. `/properties/filters/items`: the property is not
         statically reachable, so the path has to be a chain of `properties` keys and
         must not pass through `items`, `$ref`, or a composition or conditional keyword.
  warn   annotations/read-only-with-mutating-name  execute_query
         Tool `execute_query` declares `readOnlyHint: true` but its name leads with
         `execute`, which reads as a write. Either the hint or the name is misleading
         the model.
  info   x-mcp-header/present  execute_query
         Tool `execute_query` mirrors a parameter into HTTP headers. Header values are
         visible to every intermediary on the path, so nothing sensitive should be among
         them.

No tool was executed. whichtool read tools/list and stopped there.
