whichtool inspect
  target   snapshot  refs-and-defs.json
  surface  2928fb3e2a90  3 tools  ~450 tokens (estimate, heuristic-bpe-v1)

Worst first
  No errors or warnings.  2 info-level notes below.

Context cost  ~450 tokens total, mcp-neutral-compact
  tool               tokens  share  desc  schema
  create_order         ~238    53%   ~23    ~195
  get_category_tree    ~126    28%   ~23     ~82
  validate_address      ~86    19%   ~19     ~47
  Structural estimate, not a real vocabulary, and not provider-specific. Measured
  against the same four-tool surface it read 2.4x what OpenAI counted and 0.5x what
  Anthropic counted: the providers disagree with each other by more than this estimate
  disagrees with either. Use it to compare tools against each other and to spot bloat;
  for a bill, read the number your provider reports.

Annotations  3/3 tools annotated
  readOnlyHint 3/3   destructiveHint 1/3   idempotentHint 1/3   openWorldHint 1/3
  Annotations are hints, not guarantees: the MCP spec is explicit that a client must not
  rely on them as a security boundary. whichtool checks their internal consistency, not
  their truthfulness; only running the tool could establish that, and whichtool never
  does.

Lexical overlap  3 pairs scored
  No pair scores at or above 0.40. Highest: 0.26 (create_order <-> get_category_tree).

Deprecated features
  Checked against MCP 2026-07-28 (registry read 2026-08-17): none of the 6 deprecated
  features is visible in a tools/list response or a tool definition. They are all
  client-, authorization- or transport-level, so there is nothing on a surface to flag.
  The deprecated HTTP+SSE transport is refused by `--transport legacy-sse` instead.

Findings  0 errors, 0 warnings, 2 info
  info   surface/unresolved-ref-cycle  get_category_tree
         Tool `get_category_tree`: `inputSchema` keeps an unresolved `$ref`
         (#/$defs/node, cycle).
  info   surface/unresolved-ref-external  validate_address
         Tool `validate_address`: `inputSchema` keeps an unresolved `$ref`
         (https://schemas.example.com/address.json, external).

No tool was executed. whichtool read tools/list and stopped there.
